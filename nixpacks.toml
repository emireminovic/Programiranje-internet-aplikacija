[phases.build]
cmds = ["npm ci", "npm run build", "composer install --no-dev", "php artisan config:cache", "php artisan route:cache", "php artisan view:cache"]

[phases.setup]
nixPkgs = ["nodejs", "npm"]